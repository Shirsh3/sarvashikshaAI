Add Unicode fonts here for PDF export (embedded via PDFBox).

Required (preferred single universal font):
- NotoSans-Regular.ttf

Optional (explicit Devanagari font if you prefer):
- NotoSansDevanagari-Regular.ttf

These files must be present in production containers too (they are loaded from the classpath).
